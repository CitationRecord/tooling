# Anchors the rootdir here so a pytest.ini elsewhere on the machine
# cannot pull in options this repository does not use.
[pytest]
testpaths = archive/tests resolve/tests coverage/tests bulk/tests
addopts = -q
